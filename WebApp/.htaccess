
    RewriteEngine On
    RewriteBase /WebApp

    # Se il file o la cartella esistono, non riscrivere l'URL
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # Reindirizza tutto verso index.php
    RewriteRule ^ index.php [QSA,L]
